/**
 * ISO‑8601 formatted date string, e.g. "2025-12-25T13:20:00".
 * Functionally the same as a string; nothing restricted or added as it is just descriptive.
 */
export type ISODateString = string;